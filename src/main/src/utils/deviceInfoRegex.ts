export const deviceClassRegex = new RegExp(/(?<=DeviceClass: ).*/);

export const deviceNameRegex = new RegExp(/(?<=DeviceName: ).*/);

export const productTypeRegex = new RegExp(/(?<=ProductType: ).*/);

export const productVersionRegex = new RegExp(/(?<=ProductVersion: ).*/);
